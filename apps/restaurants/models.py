from apps.restaurants.domain.models import Restaurant

# este arquivo existe apenas para funcionamento do Django
# isso pois o Django sempre tenta buscar o `models.py`
# na raiz do app, não na pasta de domain